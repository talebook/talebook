---
phase: "01"
plan: "02"
type: "text-extraction"
wave: "2"
depends_on:
  - "01"
files_modified:
  - "webserver/services/tts.py"
  - "webserver/plugins/parser/txt.py"
  - "webserver/services/convert.py"
autonomous: true
requirements_addressed:
  - "TTS-02a"
  - "TTS-02b"
---

<objective>
实现文本提取（TXT/epub）和对话识别功能，复用现有解析器。
</objective>

<tasks>

<task>
<read_first>
- "webserver/plugins/parser/txt.py"
- "webserver/services/extract.py"
- "webserver/services/tts.py"
</read_first>
<action>
扩展 `webserver/services/tts.py` 中的文本提取功能，确保复用现有 TxtParser：

**1. 确认 TxtParser 可复用：**

检查 `webserver/plugins/parser/txt.py` 中的 `TxtParser` 类，确认：
- 有 `parse(file_path)` 方法返回结构化数据
- 返回的 dict 包含 `content` 或可提取内容的方式

**2. 实现 `_extract_text_from_txt()` 方法：**

```python
def _extract_text_from_txt(self, book_path: str) -> str:
    """从 TXT 文件提取文本内容

    复用 TxtParser 进行章节识别和内容提取
    """
    from webserver.plugins.parser.txt import TxtParser

    try:
        parser = TxtParser()
        result = parser.parse(book_path)

        if not result:
            return None

        # 提取纯文本内容（忽略章节标题）
        content = result.get('content', '')

        # 如果 result 有多个章节，合并内容
        if 'chapters' in result:
            chapters = result['chapters']
            if isinstance(chapters, list):
                texts = []
                for chapter in chapters:
                    if isinstance(chapter, dict):
                        # 尝试获取章节内容
                        chapter_content = chapter.get('content', chapter.get('text', ''))
                        if chapter_content:
                            texts.append(chapter_content)
                    elif isinstance(chapter, str):
                        texts.append(chapter)
                content = '\n\n'.join(texts)

        return content

    except Exception as e:
        logging.error(f"TXT extraction error: {e}", exc_info=True)
        return None
```

**3. 确认 `_extract_text()` 方法调用 TxtParser：**

更新 `_extract_text()` 中的 TXT 处理逻辑：
```python
elif ext == '.txt':
    # TXT 文件通过 TxtParser 提取
    content = self._extract_text_from_txt(book_path)
    return content
```

**4. 验证 TxtParser 接口兼容性：**

检查 `webserver/plugins/parser/txt.py` 中的 `TxtParser` 接口：

```python
# 预期接口（如果不存在，需要调整）
class TxtParser:
    def parse(self, file_path: str) -> dict:
        """
        返回结构：
        {
            'content': '...',       # 完整文本内容
            'chapters': [...],     # 章节列表（可选）
            'encoding': 'utf-8',   # 编码
        }
        """
        pass
```

如果现有 `TxtParser` 接口不同，需要调整上述代码或创建适配层。
</action>
<acceptance_criteria>
- `TxtParser.parse()` 能正确解析 TXT 文件
- `_extract_text_from_txt()` 返回纯文本内容
- TXT 文件提取不丢失章节间内容
- `grep -r "def _extract_text_from_txt" webserver/services/` 能找到方法
</acceptance_criteria>
</task>

<task>
<read_first>
- "webserver/services/convert.py"
- "webserver/services/tts.py"
</read_first>
<action>
确保 epub 文本提取通过 calibre convert 复用：

**1. 确认 ConvertService 可用：**

检查 `webserver/services/convert.py` 中的 `ConvertService` 类：
- 有 `do_ebook_convert(input_path)` 或类似方法
- 方法签名和返回类型

**2. 实现 `_extract_text_from_epub()` 方法：**

```python
def _extract_text_from_epub(self, book_path: str) -> str:
    """从 epub 文件提取文本内容

    通过 calibre convert 将 epub 转为 txt，再提取内容
    """
    from .convert import ConvertService

    try:
        convert_service = ConvertService(self.manager)

        # 使用 ConvertService 转换 epub -> txt
        txt_path = convert_service.do_ebook_convert(book_path)

        if not txt_path or not os.path.exists(txt_path):
            logging.error(f"Conversion failed: {txt_path}")
            return None

        try:
            # 读取转换后的 txt 内容
            # 尝试多种编码
            for encoding in ['utf-8', 'gbk', 'gb2312', 'latin-1']:
                try:
                    with open(txt_path, 'r', encoding=encoding) as f:
                        content = f.read()
                    break
                except UnicodeDecodeError:
                    continue
            else:
                # 所有编码都失败
                logging.error(f"Failed to decode converted txt: {txt_path}")
                content = None

            # 清理临时文件
            try:
                os.remove(txt_path)
            except Exception:
                pass

            return content

        except Exception as e:
            logging.error(f"Failed to read converted txt: {e}")
            try:
                os.remove(txt_path)
            except Exception:
                pass
            return None

    except Exception as e:
        logging.error(f"epub extraction error: {e}", exc_info=True)
        return None
```

**3. 确认 `_extract_text()` 调用 epub 提取：**

更新 `_extract_text()` 中的 epub 处理逻辑：
```python
elif ext == '.epub':
    content = self._extract_text_from_epub(book_path)
    return content
```

**4. 检查 ConvertService 接口：**

确认 `ConvertService.do_ebook_convert()` 的行为：
- 输入：书籍文件路径
- 输出：转换后的文件路径（txt/html）
- 是否清理临时文件

如果接口不同，调整上述代码。
</action>
<acceptance_criteria>
- `ConvertService.do_ebook_convert()` 存在且可用
- `_extract_text_from_epub()` 正确处理 epub 转 txt
- 转换后的 txt 文件被正确读取和清理
- `grep -r "def _extract_text_from_epub" webserver/services/` 能找到方法
</acceptance_criteria>
</task>

<task>
<read_first>
- "webserver/services/tts.py"
</read_first>
<action>
完善 `_extract_dialogues()` 方法，对话识别（Phase 1 粒度）：

**1. 对话识别实现：**

```python
def _extract_dialogues(self, text: str) -> list:
    """从文本中提取对话片段

    Phase 1 仅识别引号/标签对白，不区分说话人
    Phase 2 才做角色检测

    Returns:
        list: 对话片段列表，每项包含:
            - id: 序号
            - text: 对话文本
            - pattern_type: 匹配的模式类型
            - start: 在原文中的起始位置
            - end: 在原文中的结束位置
    """
    import re

    dialogues = []

    # 对话正则模式（按优先级）
    # 注意：长的模式要放在前面，避免短模式先匹配
    DIALOGUE_PATTERNS = [
        # 中文双引号「」
        (r'「([^」]+)」', 'zh_bracket'),
        # 中文双引号""
        (r'"([^"]+)"', 'zh_quote'),
        # 中文单引号『』
        (r'『([^』]+)』', 'zh_bracket_single'),
        # 中文单引号''
        (r"'([^']+)'", 'zh_single_quote'),
        # 英文双引号 ""
        (r'"([^"]+)"', 'en_quote'),
        # 英文单引号 ''
        (r"'([^']+)'", 'en_single_quote'),
    ]

    seen_texts = set()  # 用于去重

    for pattern, pattern_type in DIALOGUE_PATTERNS:
        for match in re.finditer(pattern, text, re.DOTALL):
            dialogue_text = match.group(1).strip()

            # 过滤无效文本
            if not dialogue_text:
                continue

            # 长度限制：5-1000 字符
            if len(dialogue_text) < 5 or len(dialogue_text) > 1000:
                continue

            # 去除可能的格式标记
            dialogue_text = self._clean_dialogue_text(dialogue_text)

            # 简单去重
            if dialogue_text in seen_texts:
                continue
            seen_texts.add(dialogue_text)

            dialogues.append({
                'id': len(dialogues),
                'text': dialogue_text,
                'pattern_type': pattern_type,
                'start': match.start(),
                'end': match.end(),
            })

    # 按原文位置排序
    dialogues.sort(key=lambda x: x['start'])

    # 合并重叠的匹配（同一位置可能被多个模式匹配）
    merged = []
    for d in dialogues:
        if not merged or d['start'] >= merged[-1]['end']:
            merged.append(d)
        else:
            # 如果重叠，保留较长的
            if len(d['text']) > len(merged[-1]['text']):
                merged[-1] = d

    # 重新编号
    for i, d in enumerate(merged):
        d['id'] = i

    return merged


def _clean_dialogue_text(self, text: str) -> str:
    """清理对话文本中的格式标记"""
    import re

    # 移除常见的格式标记
    patterns_to_remove = [
        r'\s+',  # 多余空白
        r'\[.*?\]',  # [页脚] 等
        r'\(.*?\)',  # (译者注) 等
    ]

    cleaned = text
    for pattern in patterns_to_remove:
        cleaned = re.sub(pattern, ' ', cleaned)

    # 去除首尾空白和标点
    cleaned = cleaned.strip()

    # 去除首尾的引号（如果还有）
    cleaned = cleaned.strip('""''「」『』')

    return cleaned.strip()
```

**2. 验证对话识别逻辑：**

创建测试用例验证：
```python
def test_extract_dialogues():
    """测试对话提取"""
    service = TTSService()

    test_cases = [
        # 中文引号
        ("他说：「你好」", ["你好"]),
        # 英文引号
        ('"Hello World"', ["Hello World"]),
        # 多个对话
        ('"First" 和 "Second"', ["First", "Second"]),
        # 嵌套引号（应提取外层）
        ('"外层 "内层" 外层"', ["外层 \"内层\" 外层"]),
    ]

    for text, expected in test_cases:
        dialogues = service._extract_dialogues(text)
        extracted = [d['text'] for d in dialogues]
        # 验证逻辑
```

**3. 不做角色检测的说明：**

Phase 1 对话识别仅识别引号/标签内的对白，Phase 2 才做：
- 说话人识别
- 角色名提取
- 角色信息存储
</action>
<acceptance_criteria>
- `_extract_dialogues()` 能识别中英文引号对白
- 对话列表按位置排序，无重叠
- 过滤了太短(<5)或太长(>1000)的内容
- 简单的去重逻辑避免重复提取
- `grep -r "def _extract_dialogues" webserver/services/` 能找到方法
</acceptance_criteria>
</task>

<task>
<read_first>
- "webserver/models.py"
- "webserver/services/tts.py"
</read_first>
<action>
设计 Audio 数据库表结构（Phase 1 最小字段）：

**1. 创建 Audio 模型：**

在 `webserver/models.py` 中添加（或创建 `webserver/models/audio.py`）：

```python
class Audio(db.Model):
    """音频记录表

    存储 TTS 生成的音频文件信息
    """
    __tablename__ = 'audio'

    id = db.Column(db.Integer, primary_key=True)
    book_id = db.Column(db.Integer, db.ForeignKey('books.id'), nullable=False, index=True)

    # 章节信息（可选）
    chapter_id = db.Column(db.Integer, nullable=True)

    # 文件信息
    file_path = db.Column(db.String(512), nullable=False)
    file_size = db.Column(db.Integer, nullable=True)  # bytes

    # 音频信息
    duration = db.Column(db.Float, nullable=True)  # 秒
    text_length = db.Column(db.Integer, nullable=True)  # 原始文本长度

    # 对话信息
    pattern_type = db.Column(db.String(32), nullable=True)  # 匹配的模式类型

    # 时间戳
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

    # 关系
    book = db.relationship('Book', backref=db.backref('audio_records', lazy='dynamic'))

    def __repr__(self):
        return f'<Audio {self.id}: book_id={self.book_id}>'

    def to_dict(self):
        return {
            'id': self.id,
            'book_id': self.book_id,
            'chapter_id': self.chapter_id,
            'file_path': self.file_path,
            'file_size': self.file_size,
            'duration': self.duration,
            'text_length': self.text_length,
            'pattern_type': self.pattern_type,
            'created_at': self.created_at.isoformat() if self.created_at else None,
        }
```

**2. 在 `webserver/models.py` 末尾添加导出：**

```python
# Audio 表（延迟导入避免循环）
from .audio import Audio
```

**3. 创建 `webserver/models/audio.py` 文件：**

```python
"""Audio 模型定义"""
from flask_sqlalchemy import SQLAlchemy
from datetime import datetime

db = SQLAlchemy()


class Audio(db.Model):
    """音频记录表"""
    __tablename__ = 'audio'

    id = db.Column(db.Integer, primary_key=True)
    book_id = db.Column(db.Integer, db.ForeignKey('books.id'), nullable=False, index=True)
    chapter_id = db.Column(db.Integer, nullable=True)
    file_path = db.Column(db.String(512), nullable=False)
    file_size = db.Column(db.Integer, nullable=True)
    duration = db.Column(db.Float, nullable=True)
    text_length = db.Column(db.Integer, nullable=True)
    pattern_type = db.Column(db.String(32), nullable=True)
    created_at = db.Column(db.DateTime, default=datetime.utcnow)
    updated_at = db.Column(db.DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)

    book = db.relationship('Book', backref=db.backref('audio_records', lazy='dynamic'))
```

**4. 数据库迁移：**

创建迁移脚本 `migrations/001_add_audio_table.py` 或使用 Flask-Migrate：

```bash
flask db migrate -m "Add audio table for TTS"
flask db upgrade
```
</action>
<acceptance_criteria>
- `webserver/models/audio.py` 文件存在，包含 `Audio` 模型
- `Audio` 表有以下字段：id, book_id, chapter_id, file_path, file_size, duration, text_length, pattern_type, created_at
- `Audio` 模型与 `Book` 模型有关联关系
- 数据库迁移命令可执行
- `grep -r "class Audio" webserver/models/` 能找到模型定义
</acceptance_criteria>
</task>

</tasks>

<verification>
## Verification Steps

1. **TXT 提取测试：**
   ```bash
   # 创建测试 TXT 文件
   echo '"Hello World" and "Goodbye"' > /tmp/test.txt

   # 测试 TxtParser
   cd webserver && python -c "
   from plugins.parser.txt import TxtParser
   p = TxtParser()
   r = p.parse('/tmp/test.txt')
   print('Content:', r.get('content', '')[:100])
   "
   ```

2. **epub 转换测试（需要 calibre）：**
   ```bash
   which ebook-convert
   # 如果存在，测试转换
   ```

3. **对话识别测试：**
   ```bash
   cd webserver && python -c "
   from services.tts import TTSService
   s = TTSService()
   text = '他说：「你好» and she said \"Goodbye\"'
   dialogues = s._extract_dialogues(text)
   print(f'Found {len(dialogues)} dialogues')
   for d in dialogues:
       print(f'  - [{d[\"pattern_type\"]}] {d[\"text\"]}')
   "
   ```

4. **数据库模型测试：**
   ```bash
   cd webserver && python -c "
   from models.audio import Audio
   print('Audio model imported OK')
   print('Fields:', [c.name for c in Audio.__table__.columns])
   "
   ```
</verification>

<success_criteria>
1. TXT 文件可通过 TxtParser 正确提取文本内容
2. epub 文件可通过 calibre convert 转换为 txt 并提取
3. 对话识别可匹配中英文引号对白
4. Audio 数据库表结构设计合理，包含必要字段
5. TTSService 文本提取流程完整可用
</success_criteria>

---
*Plan: 01-tts-text-extraction*
*Wave: 2*
*Requirements: TTS-02a, TTS-02b*
