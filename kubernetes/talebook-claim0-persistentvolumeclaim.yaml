apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: talebook-claim0
  name: talebook-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi